# Copie para .env e preencha. Nunca commite o .env real.

# Backend Gemma (mock | ollama | gemini) — ver backend/gemma/gemma_generate.py
#   ollama = inferência 100% LOCAL/OFFLINE (Edge AI). Instale o Ollama, rode
#            `ollama serve` e `ollama pull gemma3:4b` (texto+imagem). É o modo
#            recomendado para demonstrar Edge AI sem internet.
#   gemini = Gemma hospedado (nuvem), usado como FALLBACK quando não há modelo local.
GEMMA_BACKEND=ollama

# Backend "ollama" (LOCAL): modelo e URL. gemma3:4b faz texto+imagem no Ollama.
# Alternativas: gemma3:1b (só texto, leve) | gemma3n:e2b (texto, sem visão no Ollama).
GEMMA_MODEL=gemma3:4b
OLLAMA_URL=http://localhost:11434

# ASR OFFLINE (whisper.cpp) — transcrição 100% local, sem internet.
# Instale: brew install whisper-cpp ; baixe um modelo ggml em backend/asr/models/.
WHISPER_BIN=whisper-cli
WHISPER_MODEL=backend/asr/models/ggml-small.bin
WHISPER_LANG=pt

# Necessário só para o backend "gemini" (Gemma hospedado via Gemini API).
# Crie grátis em https://aistudio.google.com/app/apikey
GEMINI_API_KEY=

# Modelo Gemma servido pela Gemini API (opcional, tem default no código).
# Alternativa maior: gemma-4-31b-it
GEMINI_MODEL=gemma-4-26b-a4b-it

# Modelo usado APENAS para ASR (transcrição de fala) no fallback do servidor —
# NÃO é o Gemma e NÃO faz raciocínio, só converte áudio em texto quando o ASR do
# navegador (Web Speech API) não existe/falha. Ver backend/asr/transcribe.py.
# Precisa ser um modelo Gemini multimodal que aceite áudio (o Gemma rejeita áudio).
GEMINI_ASR_MODEL=gemini-flash-latest
